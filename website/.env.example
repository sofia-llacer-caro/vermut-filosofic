# Copy to .env.local (git-ignored) for local dev, and add the same values
# as repository secrets for the GitHub Pages deploy workflow to consume.
# Get these from Supabase project settings > API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
